<div class="page-wrapper">
    <div class="delete-card">

        <h2 class="mb-4">Kullanıcı Sil</h2>

        <div class="alert alert-warning">
            “<strong>@Model.Name</strong>” isimli kullanıcıyı silmek istediğinizden emin misiniz?
        </div>

        <form method="post" asp-action="Delete" asp-route-id="@Model.Id" class="d-flex gap-2">
            <button type="submit" class="btn btn-danger">Evet, Sil</button>
            <a href="/User/Index" class="btn btn-secondary">İptal</a>
        </form>

    </div>
</div>
